---
title: "example"
format: html
editor: visual
---

HEAD

This is a change I made on the `main` branch.

TESTBRANCH

This is a change I made on the `testbranch` branch.

```{r}
barplot(table(mtcars$cyl),
        col = "skyblue",
        main = "Cylinders in mtcars Dataset")
```
